# Environment Notes

- On this Windows system, use `python` not `python3` (only `python` is available at `C:/Python313/python`)
- CodeScene CLI (`cs`) is installed and uses `CS_ACCESS_TOKEN` env var for auth
- Parallel repo scanning with `&` and `wait` in bash works on Git Bash for Windows
