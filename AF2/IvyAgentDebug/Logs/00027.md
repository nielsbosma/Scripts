73d5442d-af54-457e-b2d1-d4ee66d28a58

## Session: Nexus.CrmPortal (Excel to Ivy CRM conversion)

### Outcome

Overall a very successful session. The agent converted an Excel file with 3 sheets (Contacts, Deals, Activities) into a full Ivy CRM application with Dashboard, 3 CRUD apps, EF Core database, and seed data — all in ~16 minutes. Build clean, all 15 tests passed, 47/50 spec requirements implemented (3 partial — missing app group path).

### Issues Found → Plans Created

| # | Plan | Type | Summary |
|---|------|------|---------|
| 387 | IvyFramework-Feature-Hallucination-Doc-Server-OnReady | Hallucination | Agent hallucinated `Server.OnReady` API — add to Hallucinations.md |
| 388 | Ivy-Bug-GenerateDbConnection-MigrationApplyFailure | Workflow Bug | GenerateDbConnection workflow failed at migration apply; agent recovered manually |
| 389 | IvyAgent-Bug-Agent-Retries-Blocked-DotnetRun | Agent Bug | Agent tried `dotnet run` 3x after being blocked — should stop after first denial |
| 390 | IvyAgent-Bug-Missing-App-Path-Attribute | Agent Bug | Agent doesn't set `path` on `[App]` attribute for sidebar grouping |
| 391 | IvyFramework-Bug-Backend-Parsing-Error-Null-Values | App Bug | Parsing error in backend logs from nullable field handling in dashboard metrics |

### Notable Observations

- Session used only 1 IvyQuestion (server initialization) — very efficient
- Only 1 build failure (Server.OnReady hallucination) — quick recovery
- No doc 404s, no failed questions
- Agent efficiently used reference connections for both dashboard and CRUD patterns
- 3 wasted tool calls on blocked `dotnet run`
- GenerateDbConnection workflow failure was recovered gracefully by manual EF commands

### No Tools/Memory Changed
