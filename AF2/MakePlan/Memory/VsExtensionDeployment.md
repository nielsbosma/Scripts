# VS Code Extension Deployment

The ivy-plans VS Code extension source lives at `D:\Repos\_Personal\Scripts\AF2\.vscode-extensions\ivy-plans\`.

Extensions are installed via **symlinks** — editing the source files directly updates the installed extension. No file copying is needed. Plans should instruct to reload VS Code after editing, not copy files.
