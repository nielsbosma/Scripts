In ReviewCommits.ps1 when I have selected a commit, I'd like a way to take that commit man make it as a PR to that repo instead - and then open that PR link.

## Outcome

Created plan **401** (`401-Scripts-Feature-CreatePRFromCommit.md`).

Adds a `p) Create PR from this commit` option to ReviewCommits.ps1's commit review menu. Creates a branch at the commit, pushes it, uses `gh pr create`, and opens the PR URL in the browser.

## Changes

- Created: `D:\Repos\_Ivy\.plans\401-Scripts-Feature-CreatePRFromCommit.md`
- Updated: `.counter` to 402
