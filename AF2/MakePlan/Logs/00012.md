Our MakePlan.ps1 somtimes make change directly instead of making a plan. This is not permitted! It should always make a plan.

## Outcome

Created plan `354-Scripts-Fix-MakePlanReadOnly.md` to harden MakePlan's read-only constraints. The plan proposes:

1. Adding a prominent READ-ONLY warning at the top of Program.md
2. Reinforcing constraints in Research and Rules sections
3. Exploring `--allowedTools` CLI flag to restrict tool access

## Tools

None created or changed.

## Memory

None created or changed.

## Reflection

No new learnings — this was a straightforward plan creation. The root cause (buried constraint in Program.md) was obvious from reading the file structure.
