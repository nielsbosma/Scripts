IvyAgentDebug.ps1 and it's sub step has a lot of steps.  If we already have generated a specific report we can skip that step.

## Outcome

Created plan **389** — `389-Scripts-Feature-SkipExistingReportsInDebugPipeline.md`

Adds skip logic to `IvyAgentDebug.ps1` so each sub-step (ReviewBuild, ReviewLangfuse, ReviewSpec, ReviewTests) checks for existing report files in `.ivy/` before running. Includes a `-Force` flag to override.
