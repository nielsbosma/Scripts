Look at D:\Repos\_Ivy\Ivy-Agent\Ivy.Agent\CSharp\IvyCSharpRefactoringService.cs -> StaticMethodRenames  I like the simplicity of that construct can we do that for more refactoring types? like ReplaceMethodNameOnType

## Outcome

Created plan **352** → `352-IvyAgent-Refactor-DataDrivenRefactoringRules.md`

Identified three groups of rules that share the same `(string, string, string)` tuple shape:
- `StaticMethodRenames` (already data-driven, 1 entry)
- `ReplaceMethodNameOnType` calls (7 entries → new `MethodRenamesOnType` array)
- `ReplaceEnum` calls (17 entries → new `EnumRenames` array)

Plan proposes adding two new static arrays and expanding them with `.Select()` just like the existing `StaticMethodRenames` pattern.
