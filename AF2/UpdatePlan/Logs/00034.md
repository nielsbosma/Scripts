D:\Repos\_Ivy\.plans\updating\394-General-Feature-AutoIncludeIvyAnalyser.md

## Outcome

Successfully updated plan to v2.

### Comments Applied

1. `>> Check that our analyser is deployed on release.` — Added new Section 4: "Add Ivy.Analyser NuGet publish to release workflow". Discovered that `release.yml` only creates GitHub tags/releases and has no NuGet publish step. Added `dotnet pack` and `dotnet nuget push` steps.

2. `>> Make sure we comply with LocalSource for this as well` — Updated Section 1 to add a `{{- if ProjectPath }}` branch for the analyser in `Project.tpl`, mirroring the existing Ivy framework pattern. Added Section 2 with `AnalyserProjectPath` and `AnalyserVersion` template variables in `ProjectGenerator.cs`. Added test case #2 for local-source mode verification.

3. `>> Is` — Removed (incomplete/accidental comment).

### Output File

`D:\Repos\_Ivy\.plans\394-General-Feature-AutoIncludeIvyAnalyser-v2.md`
