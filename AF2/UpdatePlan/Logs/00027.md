# UpdatePlan - Session 00027

**Args:** `D:\Repos\_Ivy\.plans\updating\384-IvyFramework-Feature-AppConstructorDI.md`

## Outcome

Successfully updated plan. Found 1 `>>` comment requesting exploration of Option B (registration-time validation).

**Comment:** `>> Let's explore Option B: How can this be done.`

**Action taken:** Researched the Ivy-Framework source code to understand the full app registration and instantiation pipeline. Rewrote the plan to focus on Option B with concrete implementation details:

- Registration-time check in `AppHelpers.GetApp()` with actionable error message
- Safety net check in `AppDescriptor.CreateApp()`
- `new()` generic constraints on `Server.UseChrome<T>()` and `Server.UseErrorNotFound<T>()` for compile-time safety

**Output:** `384-IvyFramework-Feature-AppConstructorDI-v2.md` written to `.plans/`

## Changes

- Backed up original to `.plans/history/`
- Deleted original from `.plans/updating/`
- Created versioned plan `-v2` in `.plans/`
